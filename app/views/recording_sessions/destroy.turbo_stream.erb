<%= turbo_stream.remove dom_id(@recording_session, :sidebar_item) %>
<%= turbo_stream.remove dom_id(@recording_session, :sidebar_delete_modal) %>
<%= turbo_stream.remove dom_id(@recording_session) %>
